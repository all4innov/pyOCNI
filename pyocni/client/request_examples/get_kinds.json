/*
 =1= Multiple kinds can be retrieved at the same time
 =2= If there is no filter, all kinds in the database will be retrieved
 =3= If there is a filter, it should containt only the object members described in the OCCI JSON rendering draft
 */

{
    "kinds": [
        {
            "actions": [
                "http://schemas.ogf.org/occi/infrastructure/compute/action#start",
                "http://schemas.ogf.org/occi/infrastructure/compute/action#stop"
            ]
        }
    ]
}